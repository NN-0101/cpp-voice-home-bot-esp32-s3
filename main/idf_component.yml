# ============================================================================
# IDF Component Manager Manifest File
# 组件依赖清单 - 管理项目的第三方库依赖
# ============================================================================

dependencies:
  # IDF 框架版本约束
  idf:
    version: '>=4.1.0'                    # 要求 ESP-IDF 版本不低于 4.1.0

  # 第三方组件依赖
  espressif/esp-sr: "~2.2.0"              # 乐鑫官方语音识别库，版本锁定在 2.2.x